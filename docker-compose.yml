version: "2"
services:
  node:
    build:
      context: .
      dockerfile: ./compose/nodejs/Dockerfile
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    ports:
      - "8081:8081"
    command: "npm start"